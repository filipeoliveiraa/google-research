This directory contains adjacency matrices of graphs
that provide lower bounds on certain Ramsey numbers R(r, s).
In particular, an _n_-node graph with no _r_-clique
or _s_-independent set implies a lower bound
of _n+1_ on R(_r_, _s_).

verify_bounds.ipynb verifies that the adjacency matrices
indeed prove the claimed bounds.