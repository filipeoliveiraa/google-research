## Ramsey Number Bounds

improved_bounds contains proofs of new
lower bounds on certain Ramsey numbers R(_r_, _s_),
along with a notebook that can be used
to verify the proofs.

code contains the AI-generated code used
to construct the lower bounds. Note that
in most cases the code is a randomized
procedure that is not guaranteed to find
the final lower bound with high probability.